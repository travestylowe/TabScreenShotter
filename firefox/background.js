// Background script kept minimal — popup handles all logic using
// browser.tabs.captureTab(tabId) which doesn't require tab switching.
// This file exists for future use (e.g., context menu triggers, keyboard shortcuts).

console.log('TabScreenShotter: background script loaded');
